# OS / editor
.DS_Store
Thumbs.db
*.swp
*.tmp

# Local env / secrets
.env
.env.*

# Build artifacts (keep source .md; ignore generated if added later)
*.aux
*.log
*.out

# Superseded Gamma exports — rebuild uses PPT_V3.zip only
PPT/
PPT_V2/
PPT_V3/
PPT.zip
PPT_V2.zip
